services:
  - type: web
    name: open5x-backend
    env: python
    rootDir: backend
    startCommand: "uvicorn api:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.0
